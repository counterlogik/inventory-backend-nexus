# Migration `20200418100936-add-tag-model`

This migration has been generated by addison staples at 4/18/2020, 10:09:36 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Tag" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" SERIAL,
    "ownerId" integer  NOT NULL ,
    "title" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "Tag.title" ON "public"."Tag"("title")

ALTER TABLE "public"."Tag" ADD FOREIGN KEY ("ownerId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200418095649-add-location-model..20200418100936-add-tag-model
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -20,9 +20,9 @@
   world   World  @relation(fields: [worldId], references: [id])
 }
 model Category {
-  id          Int      @id
+  id          Int      @default(autoincrement()) @id
   owner       User     @relation(fields: [ownerId], references: [id])
   ownerId     Int
   createdAt   DateTime @default(now())
   title       String   @unique
@@ -30,17 +30,25 @@
   image       String?
 }
 model Location {
-  id          Int      @id
+  id          Int      @default(autoincrement()) @id
   owner       User     @relation(fields: [ownerId], references: [id])
   ownerId     Int
   createdAt   DateTime @default(now())
   title       String   @unique
   description String?
   image       String?
 }
+model Tag {
+  id        Int      @default(autoincrement()) @id
+  owner     User     @relation(fields: [ownerId], references: [id])
+  ownerId   Int
+  createdAt DateTime @default(now())
+  title     String   @unique
+}
+
 model User {
   id        Int      @default(autoincrement()) @id
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
```


