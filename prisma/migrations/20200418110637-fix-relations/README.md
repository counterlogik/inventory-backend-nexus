# Migration `20200418110637-fix-relations`

This migration has been generated by addison staples at 4/18/2020, 11:06:37 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Category" ADD COLUMN "itemId" integer   ;

ALTER TABLE "public"."Location" ADD COLUMN "itemId" integer   ;

ALTER TABLE "public"."Tag" ADD COLUMN "itemId" integer   ;

DROP TABLE "public"."_CategoryToItem";

DROP TABLE "public"."_ItemToLocation";

DROP TABLE "public"."_ItemToTag";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200418104046-add-item-model..20200418110637-fix-relations
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -44,11 +44,11 @@
   owner       User     @relation(fields: [ownerId], references: [id])
   ownerId     Int
   createdAt   DateTime @default(now())
   title       String
-  items       Item[]   @relation(references: [id])
   description String?
   image       String?
+  itemId      Int?
   @@unique([ownerId, title])
 }
@@ -57,11 +57,11 @@
   owner       User     @relation(fields: [ownerId], references: [id])
   ownerId     Int
   createdAt   DateTime @default(now())
   title       String
-  items       Item[]   @relation(references: [id])
   description String?
   image       String?
+  itemId      Int?
   @@unique([ownerId, title])
 }
@@ -70,9 +70,9 @@
   owner     User     @relation(fields: [ownerId], references: [id])
   ownerId   Int
   createdAt DateTime @default(now())
   title     String
-  items     Item[]   @relation(references: [id])
+  itemId    Int?
   @@unique([ownerId, title])
 }
```


